apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eventhub-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: eventhub.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eventhub-service
            port:
              number: 80
